[project]
name = "diff"
version = "1.0.0"
description = "Performs diff operations on documentation in support of GitHub Actions workflows"
license = "AGPL-3.0-or-later"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "html5lib>=1.1",
    "requests>=2.34.2",
]
